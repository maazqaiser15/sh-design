import React from 'react';
import { Scheduler as ProjectScheduler } from '../../features/scheduler';

/**
 * Main Scheduler page component
 * Project timeline scheduler with Day, Week, and Month views
 */
export const Scheduler: React.FC = () => {
  return <ProjectScheduler />;
};
